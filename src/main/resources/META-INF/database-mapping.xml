<?xml version="1.0" encoding="UTF-8"?>
<entity-mappings xmlns="http://xmlns.jcp.org/xml/ns/persistence/orm"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence/orm http://xmlns.jcp.org/xml/ns/persistence/orm_2_1.xsd"
    version="2.1">
    <persistence-unit-metadata>
        <!--
            xml-mapping-metadata-complete means that all entity,
            mapped-superclasses and embeddable metadata should be picked up from
            XML (ie ignore annotations).
        -->
        <!--persistence-unit-defaults -->
            <!--
                schema / catalog will override all default definitions of schema and
                catalog in the metadata (both XML and annotations).
            -->
            <!--schema>dbo</schema>
            <catalog>EPRTRpublic</catalog>
            <access>FIELD</access>
        </persistence-unit-defaults -->
        
    </persistence-unit-metadata>
</entity-mappings>