'use strict';

describe('myApp.facilitylevels module', function() {

  beforeEach(module('myApp.facilitylevels'));

  describe('facilitylevels controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var aboutCtrl = $controller('FacilityLevelsCtrl');
      expect(aboutCtrl).toBeDefined();
    }));

  });
});